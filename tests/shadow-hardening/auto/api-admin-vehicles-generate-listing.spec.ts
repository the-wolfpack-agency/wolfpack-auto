/**
 * Auto-generated by AgenticQA SDETAgent (template fallback)
 * Route: /api/admin/vehicles/generate-listing
 * Methods: GET
 * Source: api-admin-vehicles-generate-listing
 */

import { test, expect } from "@playwright/test";

test.describe("GET /api/admin/vehicles/generate-listing", () => {
  test("returns non-500 status for valid request", async ({ request }) => {
    const resp = await request.get(`/api/admin/vehicles/generate-listing`);
    expect(resp.status(), "/api/admin/vehicles/generate-listing must not crash (500)").not.toBe(500);
    expect(resp.status(), "/api/admin/vehicles/generate-listing must not crash (502)").not.toBe(502);
  });

  test("returns JSON content-type on success", async ({ request }) => {
    const resp = await request.get(`/api/admin/vehicles/generate-listing`);
    if (resp.status() === 200) {
      expect(resp.headers()["content-type"]).toContain("application/json");
    }
    expect(resp.status()).not.toBe(500);
  });

  test("has required security headers", async ({ request }) => {
    const resp = await request.get(`/api/admin/vehicles/generate-listing`);
    if (resp.status() !== 404) {
      expect(resp.headers()["x-content-type-options"]).toBe("nosniff");
    }
  });

});