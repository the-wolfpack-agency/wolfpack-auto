# Wolfpack Auto — Session Handoff
**Date:** 2026-04-15
**HEAD commit:** `93d8854`
**Repo:** the-wolfpack-agency/wolfpack-auto

---

## Headline

Security + API foundation day: admin auth hardened with a permanent coverage guardrail, team invite emails wired end-to-end, standalone inventory CSV import shipped, OpenAPI 3.1 spec auto-generated for all 239 routes, crypto-agility wrapper with quantum-migration-ready ML-DSA slot reserved, webhook signature coverage test, RLS cross-tenant tripwire test, CSP enforce mode, public `/security-posture` page.

---

## What Shipped Today

### 1. Admin auth pass 2 — systematic coverage + 2 bugs fixed
- `src/__tests__/admin-auth-coverage.test.ts` — walks every `src/app/api/admin/**/route.ts` at test time; asserts either `requireAuth` / `requireRole` / `getServerSession` is present, or the route is in the allowlist. **Permanent drift protection.**
- `src/app/api/admin/PUBLIC_ROUTES.ts` — 5 intentionally-public routes allowlisted with per-entry reason: `accept-invite`, `reset-password`, `mfa/verify`, `digital-retail/calculator`, `digital-retail/credit-app`.
- **Bugs fixed**: `admin/system/health` (was leaking DB circuit breaker state + git commit hash + event counts without auth); `admin/vehicles/backgrounds/system/[id]` (was serving images without auth).
- Every 401 from a hardened admin route emits `system.unauthorized_access_attempt` into the learning system.
- `demo/admin-auth-pass-2-2026-04-15.md` — full report.

### 2. Team invite emails wired + inventory CSV import
- **Invite emails**: `src/lib/notifications.ts` `sendTeamInvite` now actually sends via Resend, emits `system.team_invite_sent` on success, `system.notification_send_failed` on failure (DB row is still authoritative — email is retry-safe).
- Fixed a `NEXT_PUBLIC_APP_URL` operator-precedence bug in the accept URL construction.
- `system.team_invite_accepted` event wired to the accept route.
- **Inventory CSV import**: new `POST /api/admin/inventory/import` endpoint. RFC 4180-compliant parser (handles quoted commas, e.g. `"$35,000"`), dealer-scoped (never trusts dealer_id from CSV), file size + row count caps, structured per-row error response `{ imported, skipped, errors }`. Emits `system.inventory_csv_imported`.
- 51 new tests.

### 3. OpenAPI 3.1 spec auto-generator (single source of truth)
- `scripts/generate-openapi.ts` walks `src/app/api/**/route.ts`, emits `public/openapi.json` + `docs/api.md`.
- **239 routes, 359 operations, 197 auth-required, 40 rate-limited** — all documented with auth and rate-limit flags derived from code.
- `npm run openapi` is idempotent and wired into `vercel-build` — zero drift.
- `GET /api/openapi` serves the live spec unauthenticated.
- `src/__tests__/openapi-coverage.test.ts` — fails loudly if a new route is added without regenerating. Permanent guardrail.
- `src/__tests__/openapi-valid.test.ts` — structural checks (3.1 conformance, summaries present, securitySchemes.bearerAuth).
- `system.openapi_fetched` analytics event captures consumption.
- `demo/openapi-spec-ship-2026-04-15.md` — how to view locally + hosted.

### 4. Crypto-agility wrapper (quantum-migration-ready)
- `src/lib/crypto/algorithms.ts` — named algorithm registry: `hs256`, `rs256`, `es256`, `ml-dsa-65-hybrid` (reserved, `NotImplementedError` — marketable as "quantum-migration-ready").
- `src/lib/crypto/sign.ts` — `signToken()` / `verifyToken()`.
- NextAuth `jwt` / `session` callbacks emit `system.token_signed` / `token_verified` so observability covers the one code path not wrapped (NextAuth v4 has no pluggable signing primitive).
- Every event carries `quantum_safe: boolean` — PQ adoption is measurable over time.
- Migrating to ML-DSA (NIST FIPS 204) in 2027-2029 becomes a single registry entry.

### 5. Webhook signature coverage
- `src/__tests__/webhook-signature-coverage.test.ts` — filesystem walker asserts every webhook has signature verification.
- `src/app/api/webhooks/PUBLIC_WEBHOOKS.ts` allowlist file (empty — all 3 webhooks verified).
- Audited: **resend** (Svix/HMAC), **stripe** (`constructEvent`), **twilio** (`validateTwilioSignature`). All three emit `system.webhook_signature_verified` on success and `system.webhook_signature_failed` on rejection.

### 6. RLS cross-tenant proof test
- `src/lib/__tests__/rls-cross-tenant.test.ts` — seeds dealer A + dealer B, runs representative query patterns as dealer-A user against dealer-B rows. Covers: inventory list, leads list, deals list, dealer-users list, vehicle GET/PUT.
- **Result**: no isolation bugs found. All 5 routes correctly scope to `dealer_id`. Test stays as a permanent tripwire.
- `system.cross_tenant_access_blocked` event goes into the learning system for escalation of repeat offenders.

### 7. CSP enforce + public `/security-posture` page
- CSP: `report-uri /api/csp-report` wired, `frame-ancestors 'none'`, `unsafe-eval` absent in production.
- HSTS left to Vercel edge (not duplicated in middleware).
- `/api/csp-report` — authless POST, validates shape, writes `system.csp_violation_reported`.
- `docs/security-posture.md` + `/security-posture` page — public crypto inventory, PQ migration roadmap, customer messaging.
- `scripts/verify-tls-hybrid.sh` + CI-gated test asserts `X25519MLKEM768` hybrid TLS on prod.

### 8. Dependabot cleared
- 11 stale alerts dismissed: 9 phantom peer-deps (not installed) as `not_used`, 2 already-patched (axios@1.15.0, follow-redirects@1.16.0) as `inaccurate`.
- 0 open alerts.
- Leaked PAT in the `nhomyk/AgenticQA` git remote URL was redacted + the token was revoked by the user.

---

## Commits this session

| Hash | Message |
|---|---|
| `c7ee531` | security: admin auth pass 2 — systematic coverage audit, 2 bugs fixed, permanent guardrail test |
| `8ac1fe6` | feat: auto-generate OpenAPI 3.1 spec from 239 route.ts files |
| `8f579ec` | feat: wire team invite emails with analytics + standalone inventory CSV import endpoint |
| `f59ba88` | security: CSP enforce + /security-posture + hybrid TLS verification |
| `93d8854` | security: crypto-agility wrapper + webhook coverage + RLS cross-tenant proof + fixes |

---

## Open Items / What's Next

- Wire `PROD_DOMAIN` env var in CI to activate the TLS hybrid posture assertion.
- After HSTS has been live on Vercel for >1 week, submit domains to https://hstspreload.org.
- Monitor NIST FIPS 204 (ML-DSA) Node.js library maturity; 2027 pilot inside the reserved `ml-dsa-65-hybrid` slot.
- Add `npm run openapi` equivalent handoff-scaffold script (Instinct has one; Auto doesn't yet).
- MFA on admin — still not shipped, pre-release checklist item.

---

## Known Blockers (before test client deployment)

- **`DATABASE_URL`** — Neon connection string. #1 blocker. Without it, canary fails and onboarding wizard can't persist.
- **`NEXTAUTH_SECRET`** — `openssl rand -hex 32`.
- **`NEXTAUTH_URL`** — production URL.
- **`DEALER_ID`** — for the test client.
- **`RESEND_API_KEY` + `NEXT_PUBLIC_APP_URL`** — for invite emails in production.
- **MFA on admin** — deferred to last pre-production item.
- **DEMO_MODE env var** — still live in Vercel, remove before real client data.

---

## Test State

- **2,531/2,533 passing.** 1 pre-existing `vehicle-delivery-tracker` failure (documented 2026-04-14, non-blocking), 1 skipped (CI-only TLS hybrid assertion, activates with `PROD_DOMAIN`).
- 0 new TypeScript errors.
- 197 admin routes confirmed authenticated. 5 intentionally public. 0 drift possible.
- 239 routes documented in OpenAPI. 0 drift possible.
- 3 webhooks signature-verified. 0 drift possible.

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
git pull
npx jest
npm run openapi    # regenerate spec if routes changed
```

## Where to view the API spec

- Local: `docs/api.md` (human-readable) or `public/openapi.json`
- Hosted (once deployed): `https://<dealer>.wolfpackauto.com/api/openapi`
- Security posture: `/security-posture`
