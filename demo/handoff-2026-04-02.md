# Wolfpack Auto — Session Handoff
**Date:** April 2, 2026
**HEAD commit:** `947757d` — fix: disable all auto-triggers to preserve Actions minutes
**Deployed:** https://wolfpack-auto.vercel.app
**Repo:** nhomyk/wolfpack-auto (ALWAYS cd into wolfpack-auto/ for git ops)
**Tests:** 4,100+ (118 background tests, 21 system background tests)
**Pipeline:** Manual-only until May 1 (Actions minutes budget)

---

## What Was Done Today (Day 9)

### 1. Enterprise Vehicle Background Studio (Complete)
Full rewrite of the background system from CSS-only presets to a complete photo studio pipeline:
- **7 system backgrounds** generated server-side via Sharp SVG compositing (White Studio, Dark Studio, Showroom, Outdoor Lot, Lifestyle Drive, Branded Gradient, Modern Dealership)
- **Custom upload** — drag & drop dealer backgrounds, stored in R2
- **AI background removal** — Replicate rembg + remove.bg fallback with auto-failover
- **Sharp compositing pipeline** — vehicle cutout + background + shadow + reflection + watermark
- **Batch processing** — up to 200 vehicles at once
- **Before/after toggle** on every vehicle card
- **Engagement tracking** — views, dwell time, clicks, leads per background
- **Performance analytics** — CTR, lead rate, engagement score dashboard
- **4-tab admin page**: Manage | My Backgrounds | AI Studio | Performance

### 2. Database Schema (Migration 046)
4 new tables with RLS multi-tenancy:
- `custom_backgrounds` — dealer-uploaded + system backgrounds
- `vehicle_background_assignments` — which background on which vehicle
- `background_jobs` — AI removal + compositing job queue
- `background_performance_snapshots` — time-series engagement data

### 3. New Files Created
**Core Libraries:**
- `src/lib/background-generator.ts` — rewritten: types, presets, recommendations, scoring, validation
- `src/lib/background-removal.ts` — AI provider abstraction (Replicate/remove.bg)
- `src/lib/image-compositor.ts` — Sharp compositing pipeline
- `src/lib/system-backgrounds.ts` — 7 photorealistic backgrounds via SVG compositing

**API Routes (7 new):**
- `POST /api/admin/vehicles/backgrounds/upload` — custom background upload
- `POST /api/admin/vehicles/backgrounds/remove-bg` — AI background removal
- `GET  /api/admin/vehicles/backgrounds/remove-bg` — provider health check
- `POST /api/admin/vehicles/backgrounds/composite` — vehicle + bg compositing
- `POST /api/admin/vehicles/backgrounds/batch` — batch apply (auto/preset/custom)
- `CRUD /api/admin/vehicles/backgrounds/custom/[id]` — GET/PATCH/DELETE
- `POST /api/admin/vehicles/backgrounds/engagement` — fire-and-forget tracking
- `GET  /api/admin/vehicles/backgrounds/system/[id]` — serve system bg on demand

**Tests (5 new files, 139 tests):**
- `src/lib/__tests__/background-studio.test.ts` — 46 tests
- `src/lib/__tests__/background-removal.test.ts` — 9 tests
- `src/lib/__tests__/image-compositor.test.ts` — 9 tests
- `src/lib/__tests__/system-backgrounds.test.ts` — 21 tests
- `src/lib/__tests__/background-api-contracts.integration.ts` — API contract tests

**Other:**
- `src/db/migrations/046_vehicle_backgrounds.sql`
- `scripts/seed-system-backgrounds.ts` — generates + uploads all system backgrounds
- `demo/system-backgrounds/` — preview images (PNG + WebP + thumbnails)

### 4. Pipeline Fixes
- Replaced `waitForLoadState("networkidle")` with `"load"` in 12 test files (was causing Playwright hangs)
- Added "Start production server" step before Playwright (was double-building)
- Bumped Node.js 20 → 22 in all 5 workflow files
- Fixed `cache-dependency-path` and `working-directory` references
- Added explicit timeouts to bare `waitForSelector` calls

### 5. Actions Minutes Conservation
- Disabled all auto-triggers (push, PR, schedule, deployment_status) across 6 workflows
- All workflows now manual-only (`workflow_dispatch`) until May 1 reset
- 200 minutes remaining out of 2,000

### 6. Analytics Events Added
18 new `BackgroundEvent` types in analytics-hooks.ts:
- `background.custom_uploaded/deleted/updated`
- `background.removal_started/completed/failed`
- `background.composite_started/completed/failed`
- `background.batch_composite_started/completed`
- `background.preview_generated`
- `background.assignment_created/removed`
- `background.performance_snapshot`

---

## Known Issues / In Progress

1. **System backgrounds are SVG-generated, not photorealistic** — they look like abstract gradients, not real dealership photos. Need Replicate SDXL (requires billing) or actual stock photos to replace.
2. **Replicate account has no billing** — free credits exhausted. Add payment at https://replicate.com/account/billing to enable AI background removal + image generation.
3. **Actions minutes at 90%** — all workflows manual until May 1.
4. **ESLint config** — still no `.eslintrc` (Next.js 16 migration pending).

---

## Next Steps (Priority Order)

1. **Add Replicate billing** → generate photorealistic dealership background via SDXL → replace SVG system backgrounds
2. **Wolfpack branding pass** — team feedback/wording adjustments
3. **Meghan's wireframes** → design overhaul
4. **Re-enable workflows** when Wolfpack covers Actions billing (May 1 or sooner)
5. **External pricing feeds** (KBB/NADA) for pricing engine

---

## Pre-Launch Checklist

### Ready
- [x] 61 DB tables + 4 new background tables, all migrations
- [x] 4,100+ tests, pipeline green (when run manually)
- [x] 57 analytics signals + 18 new background events
- [x] Predictive ML + self-calibration
- [x] AI pricing engine (max gross)
- [x] 7 system backgrounds + custom upload + AI compositing
- [x] VDP background generator with batch processing
- [x] Security headers (HSTS, CSP, X-Frame-Options)
- [x] PII encryption (AES-256-GCM)
- [x] MFA enforced
- [x] 13-job AgenticQA CI pipeline (manual trigger)

### Manual Steps (you do)
- [ ] Remove DEMO_MODE from Vercel env vars
- [ ] Run `node scripts/go-live.mjs`
- [ ] Custom domain + DNS
- [ ] Add Replicate billing for AI features
- [ ] R2 credentials (for file storage)
- [ ] VERCEL_TOKEN secret (for canary auto-rollback)

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
git pull
npm run dev

# Run tests
npx jest --no-coverage
npx playwright test --project=chromium

# Generate system backgrounds locally
npx tsx scripts/seed-system-backgrounds.ts --local

# Manual pipeline trigger (from GitHub Actions tab)
gh workflow run "AgenticQA Full Pipeline" --repo nhomyk/wolfpack-auto
```

---

## Stats
- **Commits today:** 6
- **New files:** 20+
- **New tests:** 139
- **Admin pages:** 75+
- **API routes:** 205+
- **Total tests:** 4,100+
- **Analytics signals:** 98+
- **Lines of code:** ~125K
