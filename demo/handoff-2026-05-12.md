# Handoff 2026-05-12

Session goal: close the mock-to-real credibility gap from the 2026-05-11 launch, and build dealer-own-data analytics that no competitor with mocked data can match. Five parallel streams, disjoint files, reserved migration numbers 069-073. All pushed to `the-wolfpack-agency/wolfpack-auto` (HEAD `0721e75`).

## What landed

| Stream | Migration | What it does | New tests |
|---|---|---|---|
| A | 069 `external_credentials` | BYO-credentials vault (AES-256-GCM) + Edmunds free public API for real valuations + Carfax/AutoCheck per-dealer proxies | 113 / 13 suites |
| B | 070 `prequal_lender_routing` | Cuts synthetic prequal offer engine. Routes prequal output to dealer's real lender contacts as a signed PDF packet via Resend. BYO-Plaid for real income verification. | 90 / 7 suites |
| C | 071 `trim_velocity_and_lead_source_views` | Per-dealer trim-velocity dashboard ("Your RAV4 XLE Hybrid sells in 11d") + lead source ROI rollup. Pure dealer-own data. | 68 / 5 suites |
| D | 072 `fi_penetration_and_tech_utilization_views` | F&I product penetration heatmap (manager × product × month) + service tech utilization grid (bay × tech × week). | 56 / 7 suites |
| E | 073 `title_lien_and_address_cache` | Real USPS address validation. State title/lien framework with honest `supported: false` returns for CA/TX/FL (no free public APIs exist; documented). | 67 / 6 suites |

**Total: 438 tests across 36 suites, all passing. 106 files, 5 migrations.**

## Action items for Nick

### Required before any of these surfaces work in prod

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
DATABASE_URL=<prod-neon-url> npm run db:migrate
```

This brings 069-073 to prod. They're additive to last session's 064-068.

### New Vercel env vars

| Var | Purpose | Required? |
|---|---|---|
| `EXTERNAL_CREDENTIAL_KEY` | 32-byte hex key for AES-256-GCM on dealer external credentials. `openssl rand -hex 32`. | YES if any dealer uses BYO. |
| `USPS_USER_ID` | Free USPS Web Tools registration. | Optional; address validation gracefully degrades without it. |

Both documented in `.env.example`.

### Optional partnerships to unlock the framework

The honest gap list:
- **KBB**: no per-dealer credential model exists; slot retained for vAuto/MMR. Edmunds is the real valuation source today.
- **Carfax-for-Dealers + AutoCheck**: `TODO(byo-shape)` markers in the clients — wire shapes need verification against a dealer's actual account.
- **Plaid**: `TODO(plaid-sdk)` — add `plaid` to package.json when ready.
- **State title/lien**: CA has no API, TX/FL are CAPTCHA-gated. Real coverage requires NMVTIS aggregator partnership (LexisNexis VINguard, Carfax-for-Dealers Title, AutoCheck) or FLHSMV Bulk Data subscription.

## What this session did NOT do

- Did not run migrations against prod. Yours to run.
- Did not run Playwright E2E (no live DB at the time). Specs are checked in.
- Did not touch operator-routes test (pre-existing failures, out of scope).
- Did not address pre-existing `reputation-monitor` flake.

## Architectural notes for next session

- `AnalyticsViewEvent` union is now established in `analytics-hooks.ts`. Future analytics dashboards extend it rather than create new unions.
- `dealer_external_credentials` is the canonical pattern for any "dealer brings their own account" integration. Future BYO providers add to the provider check constraint via a follow-on migration; no schema rework needed.
- The lender packet flow (`src/lib/prequal/lender-packet-pdf.ts`) uses dynamic `require('pdfkit')` so adding pdfkit later auto-activates PDF attachment without code changes.

## Verification

- `npx tsc --noEmit` clean for all new files (only pre-existing `operator-routes.test.ts` errors remain, unchanged from last session)
- Targeted test sweep across all 5 streams: 438/438 green in 1.8s
- Full lint clean on every new file

## Strategic context

This session's build is the direct response to my own premortem convergence point: "the demo collapses when a prospect realizes half the AI is synthetic." Three of the four marquee features from 2026-05-11 (market intel, lead enrichment, prequal) now have a real-data path:
- Market intel: Edmunds (real, free) replaces KBB mock.
- Lead enrichment: still mocked at the data layer; that's the genuine remaining gap (Clearbit/FullContact need separate work).
- Prequal: routes to real lenders the dealer already has, instead of synthetic offers.
- Recalls: was already real from last session.

Plus four dealer-own-data analytics surfaces that no DMS surfaces well. These are structural moat: only Wolfpack has the dealer's own data + the lens to interpret it.

## Bottom line

The "mock-to-real" gap is materially smaller after today. The four features from yesterday now have credible production paths. The four new analytics surfaces are real end-to-end on day one because they only use the dealer's own data.

Next session priorities, in order:
1. Run migrations 069-075 against prod.
2. Set `EXTERNAL_CREDENTIAL_KEY` + `HCAPTCHA_SECRET` in Vercel.
3. Decide whether to commit to one standalone wedge product (see CTO recommendation below) vs. continue building the full DOS as one bundle.

---

# Round 2 (afternoon): F&I Audit standalone + Literacy Ontology engine + strategic destination doc

## What landed (additive to morning's 069-073)

| Migration | What it ships | New tests |
|---|---|---|
| 074 `fi_audit_runs` | F&I Penetration Audit as a free-PDF lead magnet for the DOS sale. 4-page pdfkit-rendered audit, 20-entry industry benchmark dataset (NADA / NAF / Cox Automotive cited), public rate-limited intake with hCaptcha stub + CSV header validation, S3/R2 upload with `/tmp` fallback, Wolfpack staff console surfaces recent requests for BDC follow-up. Sample PDF committed at `public/sample-fi-audit.pdf`. | 62 / 7 suites |
| 075 `literacy_ontology` | Dealership Literacy Ontology engine — 6 tables (concepts, mappings, metrics, translations, actions, outcomes). Curated ontology shared across dealers; outcomes per-dealer with RLS. Wolfpack-staff-only authoring API. Triple-write to Neo4j makes concepts/mappings native graph data. LLM expansion stub marks `source='ai_proposed'` for human-curated approval flow. Seed of 63 entries across 7 dealership roles. | 124 / 11 suites |

**Round 2 total: 186 new tests across 18 suites, all passing. 65 new files, ~10,000 lines.**

**Session grand total: 7 migrations (069-075), 165 files, 624 tests across 54 suites.**

## Two new strategy docs

1. **`docs/fi-audit-wedge-spec-2026-05-12.md`** — the 6-8 engineer-day F&I Audit wedge spec, ICP, deliverable structure, success metrics, risks. Decision target for founder meeting.
2. **`docs/literacy-os-strategy-2026-05-12.md`** — the 18-36 month strategic destination. Frames the F&I Audit (wedge), the Spatial Translation Tool (Q3 proof), and the Literacy OS (year-2-3 destination) as a single coherent product funnel. Includes phased roadmap, hire plan implications, honest risks, founder-meeting decision tree.

## Why Literacy Ontology is in this session, not later

The ontology engine is the engineering foundation of the Spatial Translation Tool AND the year-2 role-specific dashboards AND the year-3 onboarding/training surfaces. Building the engine first (today) means: (a) the F&I Audit's recommendations can render through the ontology when the founder green-lights Q3, (b) the year-2 build is unblocked, (c) the "you need a senior content hire to author 500 mappings" concern is reframed to "we have the authoring engine + AI-assist; we seed-author with CTO+CEO content-design sessions until the hire is justified." Strategic prep at low cost while founder meeting decides whether to accelerate.

## Required deployment actions (additive)

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
DATABASE_URL=<prod-neon-url> npm run db:migrate     # brings 069-075 to prod
DATABASE_URL=<prod-neon-url> npx tsx scripts/seed-literacy-ontology.ts
```

The seed is idempotent (upserts by slug).

### New Vercel env vars (additive)

| Var | Purpose | Required? |
|---|---|---|
| `HCAPTCHA_SECRET` | hCaptcha verification on public F&I Audit intake | YES before public launch; stubbed with warning when missing |

## New action items consolidated

1. Run migration 075 (+ all earlier).
2. Run `scripts/seed-literacy-ontology.ts` to bootstrap 63 starter mappings.
3. Set `HCAPTCHA_SECRET` in Vercel before publishing the audit landing page.
4. Founder meeting decisions (per `literacy-os-strategy-2026-05-12.md`):
   - **D1:** Ship F&I Audit by May 31? Y/N.
   - **D2:** Commit to Literacy OS as year-1-3 destination? Y/N.
   - **D3:** Authorize the two customer-discovery stress tests within 14 days? Y/N.
5. Two-week stress tests (if D3 = yes):
   - One dealer states a $X/month willingness-to-pay for the role-specific translation concept.
   - One dealership employee says "yes I'd use this in my daily work" after walkthrough.

## What this session did NOT do

- Did not run migrations against prod (yours).
- Did not run Playwright E2E (no live DB).
- Did not wire a real LLM provider into the ontology expansion stubs (deliberate — TODO markers in place).
- Did not productionize hCaptcha (env var stubbed with safe fallback).
- Did not address `operator-routes.test.ts` pre-existing errors (separate triage).

## Architectural notes for next session

- **Ontology authoring is API-only this round.** No UI. The `/api/admin/literacy/*` routes accept CRUD against the six entity types and gate on Wolfpack staff role (admin/operator). Authoring UI is Q3 work.
- **F&I Audit triple-writes to Qdrant + Neo4j on submission.** That means we can already do semantic similarity over audit prospects (e.g., "show me audit requests similar to this one we converted last month"). Useful as the pipeline grows.
- **Neo4j is now load-bearing in two ways:** as the rendering backend for the literacy ontology graph, and as the triple-write target for audit-lead similarity. Confirm `NEO4J_URI` is live in prod before either feature is depended on operationally.
- **`AnalyticsViewEvent` is established convention.** Two new event unions added this round: `FIAuditEvent` (7 events, 7-stage funnel) and `LiteracyEvent` (17 events covering CRUD across all six entity types + rendering + outcome recording).

## Final state

- HEAD: `d29d2ed` pushed to `the-wolfpack-agency/wolfpack-auto`
- Vercel will auto-deploy
- `npm run build` green
- `tsc --noEmit` clean (operator-routes pre-existing errors only)
- 624 tests / 54 suites added today, all passing
- 3 strategy docs in `docs/` for the founder meeting

---

# Round 3 (evening): team-led GTM reframe and full strategic doc set

The afternoon GTM assumptions (cold-outbound to F&I Directors, hire a sales lead) were wrong for Wolfpack's actual leverage. The team is a domain-led agency with multi-year OEM relationships (PCNA JD Power awards), broadcast-quality video production, senior trainer and curriculum design, master-designer talent, hospitality-grade event delivery, and cross-OEM industry connections. This is fundamentally different from a tech-startup PMF hunt. The strategic docs have been updated to reflect the actual team and the OEM-led GTM motion that fits it.

## New strategic docs (foundational, in docs/)

1. **`wolfpack-team-capabilities-2026-05-12.md`** — anchor doc cataloging team strengths and product-seat implications. Hoxsie (OEM lead PCNA), Jorge (Audi/Kia + cross-OEM), Zocchi (trainer / curriculum / Literacy OS author), Max (video / content production), Megan (master designer), Alicia (PM / hospitality / training events), Homyk (platform / AI). Every other doc references this.
2. **`dealer-excellence-program-2026-05-12.md`** — productized integrated services+software offering. Annual engagement structure: onboarding wave → training delivery → content production → ongoing optimization → renewal motion. Each team member has a named role on the critical path. Pricing model is program-engagement, not per-seat SaaS.
3. **`oem-led-gtm-strategy-2026-05-12.md`** — supersedes the cold-outbound assumptions in earlier docs. Primary motion runs through OEM relationships the team already has. PCNA likely lighthouse, parallel pipeline (Audi/Kia/Toyota/others) developed simultaneously. Direct-to-dealer accepted as inbound, not pursued as outbound. CDK/Cox partnership posture: case-by-case based on terms.

## Updated strategic docs

4. **`literacy-os-strategy-2026-05-12.md`** — content-author hire concern dissolved by naming Zocchi as lead, Megan as design lead, Alicia as event-delivery lead. Four structural advantages now include JD Power proof point and the unique team composition.
5. **`fi-audit-wedge-spec-2026-05-12.md`** — reframed as engagement-opener (OEM pitch artifact AND direct-dealer inbound capture), not cold-outbound SaaS wedge. Sales motion now OEM-led primary + inbound-secondary. Megan design pass required before public release.

## Newly identified product surfaces (year-one Q3-Q4 priority)

The team capabilities surfaced two new platform surfaces that did not exist in earlier roadmaps:

- **Media library + content production workflow** for Max's broadcast-grade video assets. Brand-compliance review workflow, OEM-themed asset management, multi-rooftop distribution.
- **Training-event infrastructure** for Alicia's hospitality-grade event delivery. Scheduling, registration, attendee tracking, completion certifications, post-event follow-up.

Both are Q3-Q4 work; stubbed data models in Q2 to establish the foundation.

## Revised hire plan (supersedes earlier action items)

The "dealer-industry sales lead" hire that was the #1 morning action item is deferred. The team already has OEM-relationship coverage. New year-one hire priorities (per `oem-led-gtm-strategy-2026-05-12.md`):

1. Program delivery lead reporting to Alicia (once a signed OEM engagement justifies it).
2. Content production coordinator reporting to Max (once multi-rooftop deployment justifies it).
3. Additional curriculum designer reporting to Zocchi (once the ontology engine is in active production use).
4. Sales lead only if direct-dealer secondary motion grows enough to justify dedicated capacity (probably not year one).

## Founder-meeting decision tree (revised, supersedes prior)

**D1:** Adopt OEM-led GTM as primary motion, direct-to-dealer as secondary opportunity-driven? (User has provisionally indicated Y.)
**D2:** Approve PCNA as primary lighthouse target while developing parallel pipeline? (User has provisionally indicated Y with caveat that nothing is off the table.)
**D3:** CDK / Cox partnership posture? (User indicated case-by-case based on whether it benefits or harms.)
**D4:** Adopt Dealer Excellence Program as productized offering structure?
**D5:** Authorize three stress tests within 30 days (one OEM-contact validation, one dealer-principal validation, one platform-readiness review)?

## What did not change

- All engineering work shipped today (migrations 069-075, 624 tests) holds up under the team-led reframe. The platform is the right spine.
- The Literacy Ontology engine is correct foundational work whether the founder meeting commits to the full program model or scales back to a wedge product.
- The F&I Audit ships in both motions; only the pitch positioning and follow-up flow differ.

## Required deployment actions (unchanged from prior rounds)

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
DATABASE_URL=<prod-neon-url> npm run db:migrate     # brings 069-075 to prod
DATABASE_URL=<prod-neon-url> npx tsx scripts/seed-literacy-ontology.ts
```

Plus: `EXTERNAL_CREDENTIAL_KEY` and `HCAPTCHA_SECRET` in Vercel.

## Round 3 doc-update commit

This evening round was doc-only. No new code, no migrations. Updates and adds in `docs/` only. Five strategy docs total in the founder-meeting packet:

1. `wolfpack-team-capabilities-2026-05-12.md` (NEW)
2. `dealer-excellence-program-2026-05-12.md` (NEW)
3. `oem-led-gtm-strategy-2026-05-12.md` (NEW)
4. `literacy-os-strategy-2026-05-12.md` (UPDATED)
5. `fi-audit-wedge-spec-2026-05-12.md` (UPDATED)
6. `handoff-2026-05-12.md` (this doc, UPDATED to summarize all three rounds)

## Bottom line (revised)

Morning closed the mock-to-real demo gap. Afternoon shipped the wedge product (F&I Audit) AND the engine for the strategic destination (Literacy Ontology), so the founder meeting can make commitment decisions against actually-existing infrastructure rather than slideware. The Audit is ready to publish behind one env var (`HCAPTCHA_SECRET`). The ontology is ready to author into. The strategy doc lays out the year-1-3 path with explicit gates.

Founder meeting now decides whether we accelerate into Q3 Spatial Translation v1, or pause at year-one Q2 having shipped a credible standalone product.
