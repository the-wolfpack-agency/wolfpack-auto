# Handoff 2026-05-12

Session goal: close the mock-to-real credibility gap from the 2026-05-11 launch, and build dealer-own-data analytics that no competitor with mocked data can match. Five parallel streams, disjoint files, reserved migration numbers 069-073. All pushed to `the-wolfpack-agency/wolfpack-auto` (HEAD `0721e75`).

## What landed

| Stream | Migration | What it does | New tests |
|---|---|---|---|
| A | 069 `external_credentials` | BYO-credentials vault (AES-256-GCM) + Edmunds free public API for real valuations + Carfax/AutoCheck per-dealer proxies | 113 / 13 suites |
| B | 070 `prequal_lender_routing` | Cuts synthetic prequal offer engine. Routes prequal output to dealer's real lender contacts as a signed PDF packet via Resend. BYO-Plaid for real income verification. | 90 / 7 suites |
| C | 071 `trim_velocity_and_lead_source_views` | Per-dealer trim-velocity dashboard ("Your RAV4 XLE Hybrid sells in 11d") + lead source ROI rollup. Pure dealer-own data. | 68 / 5 suites |
| D | 072 `fi_penetration_and_tech_utilization_views` | F&I product penetration heatmap (manager × product × month) + service tech utilization grid (bay × tech × week). | 56 / 7 suites |
| E | 073 `title_lien_and_address_cache` | Real USPS address validation. State title/lien framework with honest `supported: false` returns for CA/TX/FL (no free public APIs exist; documented). | 67 / 6 suites |

**Total: 438 tests across 36 suites, all passing. 106 files, 5 migrations.**

## Action items for Nick

### Required before any of these surfaces work in prod

```bash
cd /Users/nicholashomyk/mono/AgenticQA/wolfpack-auto
DATABASE_URL=<prod-neon-url> npm run db:migrate
```

This brings 069-073 to prod. They're additive to last session's 064-068.

### New Vercel env vars

| Var | Purpose | Required? |
|---|---|---|
| `EXTERNAL_CREDENTIAL_KEY` | 32-byte hex key for AES-256-GCM on dealer external credentials. `openssl rand -hex 32`. | YES if any dealer uses BYO. |
| `USPS_USER_ID` | Free USPS Web Tools registration. | Optional; address validation gracefully degrades without it. |

Both documented in `.env.example`.

### Optional partnerships to unlock the framework

The honest gap list:
- **KBB**: no per-dealer credential model exists; slot retained for vAuto/MMR. Edmunds is the real valuation source today.
- **Carfax-for-Dealers + AutoCheck**: `TODO(byo-shape)` markers in the clients — wire shapes need verification against a dealer's actual account.
- **Plaid**: `TODO(plaid-sdk)` — add `plaid` to package.json when ready.
- **State title/lien**: CA has no API, TX/FL are CAPTCHA-gated. Real coverage requires NMVTIS aggregator partnership (LexisNexis VINguard, Carfax-for-Dealers Title, AutoCheck) or FLHSMV Bulk Data subscription.

## What this session did NOT do

- Did not run migrations against prod. Yours to run.
- Did not run Playwright E2E (no live DB at the time). Specs are checked in.
- Did not touch operator-routes test (pre-existing failures, out of scope).
- Did not address pre-existing `reputation-monitor` flake.

## Architectural notes for next session

- `AnalyticsViewEvent` union is now established in `analytics-hooks.ts`. Future analytics dashboards extend it rather than create new unions.
- `dealer_external_credentials` is the canonical pattern for any "dealer brings their own account" integration. Future BYO providers add to the provider check constraint via a follow-on migration; no schema rework needed.
- The lender packet flow (`src/lib/prequal/lender-packet-pdf.ts`) uses dynamic `require('pdfkit')` so adding pdfkit later auto-activates PDF attachment without code changes.

## Verification

- `npx tsc --noEmit` clean for all new files (only pre-existing `operator-routes.test.ts` errors remain, unchanged from last session)
- Targeted test sweep across all 5 streams: 438/438 green in 1.8s
- Full lint clean on every new file

## Strategic context

This session's build is the direct response to my own premortem convergence point: "the demo collapses when a prospect realizes half the AI is synthetic." Three of the four marquee features from 2026-05-11 (market intel, lead enrichment, prequal) now have a real-data path:
- Market intel: Edmunds (real, free) replaces KBB mock.
- Lead enrichment: still mocked at the data layer; that's the genuine remaining gap (Clearbit/FullContact need separate work).
- Prequal: routes to real lenders the dealer already has, instead of synthetic offers.
- Recalls: was already real from last session.

Plus four dealer-own-data analytics surfaces that no DMS surfaces well. These are structural moat: only Wolfpack has the dealer's own data + the lens to interpret it.

## Bottom line

The "mock-to-real" gap is materially smaller after today. The four features from yesterday now have credible production paths. The four new analytics surfaces are real end-to-end on day one because they only use the dealer's own data.

Next session priorities, in order:
1. Run migrations 069-073 against prod.
2. Set `EXTERNAL_CREDENTIAL_KEY` in Vercel.
3. Decide whether to commit to one standalone wedge product (see CTO recommendation below) vs. continue building the full DOS as one bundle.
