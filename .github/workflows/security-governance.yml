name: Security Governance

on:
  schedule:
    - cron: '30 9 * * 1'
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  governance:
    uses: the-wolfpack-agency/AgenticQA-core/.github/workflows/security-governance.yml@main
    with:
      repo: ${{ github.repository }}
      max_age_days: 90
    secrets: inherit
