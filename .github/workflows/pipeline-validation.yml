name: Pipeline Self-Validation

on:
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  validate:
    uses: the-wolfpack-agency/AgenticQA-core/.github/workflows/pipeline-validation.yml@main
    with:
      severity: 'medium'
      fail_on_findings: 'false'
    secrets: inherit
