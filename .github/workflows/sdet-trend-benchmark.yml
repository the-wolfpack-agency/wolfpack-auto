name: SDET Trend Benchmark

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

jobs:
  benchmark:
    uses: the-wolfpack-agency/AgenticQA-core/.github/workflows/sdet-trend-benchmark.yml@main
    with:
      jest_command: 'npx jest --json --silent'
      node_version: '22'
      working_directory: '.'
      install_command: 'npm ci'
    secrets: inherit
